{"locations":[
<% if geocaches.nil? || geocaches.empty? 
     #do nothing
   else
       geocaches.each do |geocache|
%>
<%= "{\"id\":\"#{geocache.id}\",\"lat\":\"#{ sprintf "%.6f", geocache.lat}\",\"lon\":\"#{ sprintf "%.6f", geocache.lon}\"}" -%>
<%
       end
   end
%>
]}
